import Skills from '../components/Skills';

export default function SkillsPage() {
  return (
    <div className="skills-page" style={{ minHeight: '100vh', padding: '2rem 0', background: 'var(--bg-primary)' }}>
      <Skills />
    </div>
  );
}
